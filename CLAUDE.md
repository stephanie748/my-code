# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty scaffolding: it contains only a `README.md` (`# my-code`) and no source code, build tooling, tests, or configuration. There is no established architecture, command set, or convention to document yet.

When code is added to this repository, update this file to reflect:
- Build, lint, and test commands (including how to run a single test)
- The high-level architecture and how major components fit together
- Project-specific conventions not already obvious from the code itself
